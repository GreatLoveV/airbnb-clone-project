# Seed Database with Sample Data

This folder contains the SQL script to populate the database with sample data for testing and development purposes.

## Usage

To seed the database, run the `seed.sql` script in your PostgreSQL database:

```bash
psql -f seed.sql
